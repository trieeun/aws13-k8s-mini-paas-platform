apiVersion: v1
kind: Service
metadata:
  name: svc-{{ app_name }}
  namespace: ns-{{ app_name }}
spec:
  type: ClusterIP
  selector:
    app: {{ app_name }}
  ports:
  - port: 80
    targetPort: {{ port }}
