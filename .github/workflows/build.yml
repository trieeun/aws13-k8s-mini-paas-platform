name: Build and Deploy

on:
  push:
    branches:
      - main                        # main 브랜치 push 시 실행

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}   # owner/repo 형식 자동 설정
  APP_NAME: ${{ github.event.repository.name }}  # 레포 이름 = 앱 이름

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # ── 1. 코드 체크아웃 ──────────────────────────────
    - name: Checkout
      uses: actions/checkout@v4.2.2

    # ── 2. Dockerfile 존재 확인 ───────────────────────
    - name: Check Dockerfile
      run: |
        if [ ! -f Dockerfile ]; then
          echo "Dockerfile이 없습니다. 배포를 중단합니다."
          exit 1
        fi
        echo "Dockerfile 확인 완료"

    # ── 3. ghcr.io 로그인 ─────────────────────────────
    - name: Login to ghcr.io
      uses: docker/login-action@v3.4.0
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    # ── 4. Docker 이미지 빌드 + Push ──────────────────
    - name: Build and Push image
      uses: docker/build-push-action@v6.16.0
      with:
        context: .
        push: true
        tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

    # # ── 5. FastAPI 배포 API 호출 ──────────────────────
    # - name: Trigger Deploy API
    #   run: |
    #     RESPONSE=$(curl -s -o /tmp/response.json -w "%{http_code}" \
    #       -X POST ${{ secrets.PAAS_API_URL }}/deploy \
    #       -H "Content-Type: application/json" \
    #       -d "{
    #         \"app_name\": \"${{ env.APP_NAME }}\",
    #         \"image\": \"${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest\",
    #         \"port\": 8080
    #       }")

    #     echo "HTTP Status: $RESPONSE"
    #     cat /tmp/response.json

    #     # 200, 201 외 응답이면 실패 처리
    #     # 400(중복배포)은 이미 배포된 것이므로 성공으로 처리
    #     if [ "$RESPONSE" != "200" ] && [ "$RESPONSE" != "400" ]; then
    #       echo "배포 API 호출 실패"
    #       exit 1
    #     fi

    #     echo "배포 완료"
    #     # 반환된 URL 출력
    #     URL=$(cat /tmp/response.json | python3 -c "import sys,json; print(json.load(sys.stdin).get('url',''))")
    #     echo "배포 URL: $URL"

    # # ── 6. 빌드 실패 시 알림 ──────────────────────────
    # - name: Notify on failure
    #   if: failure()
    #   run: |
    #     echo "빌드/배포 실패"
    #     echo "레포: ${{ github.repository }}"
    #     echo "커밋: ${{ github.sha }}"
    #     echo "브랜치: ${{ github.ref_name }}"